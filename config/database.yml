# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
  adapter: mysql
  database: threatredigsbooker_dev

test:
  adapter: mysql
  database: threatredigsbooker_test

production:
  adapter: mysql
  database: threatredigsbooker
